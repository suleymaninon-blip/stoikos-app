name = "stoikos-backend"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Hafıza deposu (KV). Aşağıdaki id'yi `wrangler kv namespace create MEMORY`
# komutunu çalıştırınca aldığın gerçek id ile değiştir.
[[kv_namespaces]]
binding = "MEMORY"
id = "BURAYA_KV_ID_YAZILACAK"

# ANTHROPIC_API_KEY bir SIR'dır, buraya YAZILMAZ.
# Şu komutla eklenir:  wrangler secret put ANTHROPIC_API_KEY
